import { NextPage } from "next";

const Account: NextPage = () => {
  return (
    <div className="flex flex-grow flex-col items-center justify-center">
      <h1 className="mb-6 text-3xl font-bold">Account Page</h1>
    </div>
  );
};

export default Account;
