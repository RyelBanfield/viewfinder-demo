import { NextPage } from 'next';

const Home: NextPage = () => (
  <main className="flex flex-grow flex-col items-center justify-center p-3">
    <h1 className="mb-6 text-xl font-bold">Powered by creators everywhere.</h1>
  </main>
);

export default Home;
